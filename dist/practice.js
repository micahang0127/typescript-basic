"use strict";
var message = "hello";
console.log(message);
var c = 0;
c += 1;
//  c = "hh";  // error
var numbers = [1, 2, 3]; // 숫자 배열
var messages = ['hello', 'world']; // 문자열 배열
// messages.push(1); // error
var mightBeUndefined = undefined; // string, undefined 가능
var nullableNumber = null; // number, null 가능
var color = 'red'; // red, orange, yellow 중 하나의 값
// color = 'green';  // error
